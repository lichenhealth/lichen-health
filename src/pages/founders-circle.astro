---
import BaseLayout from '../layouts/BaseLayout.astro';
import FoundersForm from '../components/FoundersForm.astro';
import { tiers, steps, calendar, faqs } from '../data/founders';
const heroImg = '/founders-hero.jpg';
---
<BaseLayout overHero={true} title="The Founders Circle — Lichen" description="The Founders Circle is the inner cohort whose contributions bring Lichen into being — anchoring the first land, the first maloka, and the first generation of healing.">
  <section class="phero">
    <div class="phero-bg"><img src={heroImg} alt="" /></div>
    <div class="container phero-inner">
      <span class="eyebrow proj-eyebrow">Phase 1 Founding Cohort</span>
      <h1>The Founders Circle</h1>
    </div>
  </section>

  <section class="fsection intro-section">
    <div class="container">
      <p class="intro-lead">Beneath every old-growth forest is a quiet architecture — mycelium sharing water, nutrients, and medicine between trees so the whole canopy may thrive. The Founders Circles are the foundational network that ensures the forest will thrive and its trees will stand tall.</p>
      <p class="intro-note">An inner cohort of patrons, stewards and quiet believers who anchor the first land, the first maloka, the first version of the Lichen platform, and the first generation of healing offered. A foundational community where membership isn't transactional — it's a relationship, carrying lifetime recognition, year-round invitations, connection with like-hearted patrons, and a direct line to the founding team.</p>
      <hr class="intro-rule" />
    </div>
  </section>

  <section class="fsection">
    <div class="container container--narrow">
      <div class="sec-head"><span class="eyebrow">Two ways to take part</span><h2 class="display h-2">Give now, or pledge toward the build</h2></div>
      <div class="give-paths">
        <div class="give-path" data-contribution="Give now" role="button" tabindex="0">
          <h3 class="give-path-t">Give now</h3>
          <p>Make a contribution today and impact access to care immediately. Your gift flows straight into the work already underway — including the initial platform build and the scholarship fund that brings healers, veterans, and frontline workers onto the land at no cost.</p>
          <span class="give-path-cta">Give now →</span>
        </div>
        <div class="give-path" data-contribution="Pledge toward the build" role="button" tabindex="0">
          <h3 class="give-path-t">Pledge toward the build</h3>
          <p>Commit a larger sum toward building out the location, platform and programming. We'll keep you close and, once we reach the critical mass of funds needed to begin, we'll let you know — and translate your pledge into a donation at that moment.</p>
          <span class="give-path-cta">Pledge →</span>
        </div>
      </div>
    </div>
  </section>

  <section class="fsection" id="paths">
    <div class="container container--narrow">
      <div class="sec-head"><span class="eyebrow">Two forms of thanks</span><h2 class="display h-2">Name the true value of everything</h2></div>
      <p class="paths-lead">Lichen's economy is built on one discipline: name the true value of everything. We apply it to ourselves first — so our gratitude comes in two forms, and we're transparent about the difference.</p>
      <div class="thanks-forms">
        <div class="thanks-form">
          <h3 class="thanks-form-t">Recognition &amp; Relationship</h3>
          <p>Your name in the land's permanent record, your voice in our circle, your place at community gatherings, a direct line to the founding team. These carry no market value the IRS can price — which means they attach to <em>every</em> gift, however you give, and your full contribution remains deductible.</p>
        </div>
        <div class="thanks-form">
          <h3 class="thanks-form-t">Founders' Experiences</h3>
          <p>Ceremonies, retreats, consultations, private dinners. These are real things with real value — and a conscious economy doesn't pretend otherwise. Experiences are available as an <em>election</em> on gifts made from personal funds: we state each experience's fair market value in writing before you commit, and your deductible amount is your gift minus that value. Nothing hidden, nothing inflated. It's simply the honest price of the honey.</p>
        </div>
      </div>
      <p class="paths-choose"><strong>Every founder chooses their path at commitment:</strong></p>
      <ul class="path-list">
        <li><strong>Full Deduction path</strong> — Recognition &amp; Relationship only. Your entire gift is deductible. <em>This is the required path for gifts made through donor-advised funds, private foundations, and IRA qualified charitable distributions — IRS rules do not permit personal benefits on these giving vehicles, and we protect your tax position as carefully as our own.</em></li>
        <li><strong>Experiences path</strong> — Recognition &amp; Relationship plus your tier's elected experiences, on personal-fund gifts, with written value disclosure. Many founders choose this and consider the non-deductible portion the best money they spend all year.</li>
      </ul>
      <p class="paths-switch">You may switch to the Full Deduction path at any time by waiving future experiences.</p>
    </div>
  </section>

  <section class="fsection" id="tiers">
    <div class="container">
      <div class="sec-head"><span class="eyebrow">Membership tiers</span><h2 class="display h-2">Each tier, a deeper layer of the forest</h2></div>
      <div class="tier-grid">
        {tiers.map((t) => (
          <div class="ftier" data-tier={t.name} role="button" tabindex="0">
            <span class="ftier-from">{t.from}</span>
            <h3 class="ftier-name">{t.name}</h3>
            <p class="ftier-essence">{t.essence}</p>
            <div class="ftier-sub"><span class="ftier-sublabel">Recognition &amp; Relationship</span><span class="ftier-subnote">all gift types</span></div>
            <ul class="ftier-benefits">{t.recognition.map((b) => <li>{b}</li>)}</ul>
            <div class="ftier-sub"><span class="ftier-sublabel">Elected Experiences</span><span class="ftier-subnote">personal-fund gifts; value disclosed in writing</span></div>
            <ul class="ftier-benefits ftier-benefits--exp">{t.experiences.map((b) => <li>{b}</li>)}</ul>
            <span class="ftier-choose">Choose this tier →</span>
          </div>
        ))}
      </div>
      <p class="tier-foot">Contributions compound — a member at one level is welcomed into the next as commitments accumulate, across all giving vehicles. Anonymity is honored at every tier. Before any transfer of funds, you receive in writing: confirmation of your tier, your chosen path, and — on the Experiences path — the stated fair market value of each elected experience and your resulting deductible amount.</p>
    </div>
  </section>

  <section class="fband">
    <div class="container container--narrow band-center">
      <h2>Where your contribution lives</h2>
      <p>Founders Circle commitments fund the Phase 1 land, the maloka, the first guest structures, the founding clinical and ceremonial program, the Phase 1 tech platform — and the scholarship fund that brings healers, veterans, and frontline workers onto the land at no cost.</p>
    </div>
  </section>

  <section class="fsection">
    <div class="container">
      <div class="sec-head"><span class="eyebrow">How to join</span><h2 class="display h-2">It begins with a conversation</h2></div>
      <div class="steps">
        {steps.map((s) => (
          <div class="step"><span class="step-n">{s.n}</span><h3 class="step-t">{s.t}</h3><p>{s.d}</p></div>
        ))}
      </div>
    </div>
  </section>

  <section class="fsection">
    <div class="container container--narrow">
      <div class="sec-head"><span class="eyebrow">The year on the land</span><h2 class="display h-2">Annual convergence calendar</h2></div>
      <table class="cal">
        <thead><tr><th>Month</th><th>Gathering</th><th>Access</th></tr></thead>
        <tbody>{calendar.map(([m, g, a]) => <tr><td class="cal-m">{m}</td><td>{g}</td><td class="cal-a">{a}</td></tr>)}</tbody>
      </table>
      <p class="cal-foot">All-tier gatherings are community events, open in spirit and free of charge. Where a gathering has a ticketed equivalent (e.g., the Founders' Gala), its fair market value is disclosed to attending founders in writing.</p>
    </div>
  </section>

  <section class="fsection form-section" id="begin">
    <div class="container container--narrow">
      <hr class="intro-rule" style="margin-bottom: var(--s-8)" />
      <span class="eyebrow">Take your place in the Circle</span>
      <h2 class="display h-1" style="margin: var(--s-2) 0 var(--s-4)">Begin the conversation</h2>
      <p class="form-lede">The Founders Circle is intentionally small — a sanctuary, not a subscription. Share a little about yourself and the founding team will reach out personally.</p>
      <FoundersForm />
      <p class="contact-line">Or reach us directly: <a href="mailto:founders@lichen.health">founders@lichen.health</a></p>
    </div>
  </section>

  <section class="fsection">
    <div class="container container--narrow">
      <div class="sec-head"><span class="eyebrow">Questions</span><h2 class="display h-2">Good to know</h2></div>
      <div class="faqs">{faqs.map((f) => <div class="faq"><h3 class="faq-q">{f.q}</h3><p class="faq-a">{f.a}</p></div>)}</div>
    </div>
  </section>
</BaseLayout>
<script>
  const select = document.getElementById('tierSelect') as HTMLSelectElement | null;
  const target = document.getElementById('begin');
  const choose = (card: Element) => {
    const tier = (card as HTMLElement).dataset.tier;
    if (select && tier) {
      const opt = Array.from(select.options).find((o) => o.text.startsWith(tier));
      if (opt) select.value = opt.value;
    }
    target?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    select?.classList.add('flash');
    setTimeout(() => select?.classList.remove('flash'), 1300);
  };
  const radios = document.querySelectorAll('input[name="contribution"]') as NodeListOf<HTMLInputElement>;
  const chooseContribution = (card: Element) => {
    const val = (card as HTMLElement).dataset.contribution;
    radios.forEach((r) => { if (r.value === val) r.checked = true; });
    target?.scrollIntoView({ behavior: 'smooth', block: 'start' });
  };
  document.querySelectorAll('.give-path').forEach((card) => {
    card.addEventListener('click', () => chooseContribution(card));
    card.addEventListener('keydown', (e) => {
      const k = (e as KeyboardEvent).key;
      if (k === 'Enter' || k === ' ') { e.preventDefault(); chooseContribution(card); }
    });
  });
  document.querySelectorAll('.ftier').forEach((card) => {
    card.addEventListener('click', () => choose(card));
    card.addEventListener('keydown', (e) => {
      const k = (e as KeyboardEvent).key;
      if (k === 'Enter' || k === ' ') { e.preventDefault(); choose(card); }
    });
  });
</script>

<style>
  .proj-eyebrow { color: var(--peach-light); }
  .intro-section { padding-bottom: var(--s-6); }
  .intro-lead { font-size: clamp(21px,2.5vw,27px); line-height: 1.5; color: var(--ink); font-weight: 300; max-width: 64ch; font-family: var(--font-display); }
  .intro-note { font-size: var(--t-lg); line-height: var(--lh-loose); color: var(--ink-soft); font-weight: 300; margin-top: var(--s-6); max-width: 64ch; }
  .intro-rule { height: 1px; background: #12181C; border: 0; margin-top: var(--s-8); }

  .sec-head { display: flex; flex-direction: column; gap: var(--s-2); margin-bottom: var(--s-7); }

  .paths-lead { font-size: var(--t-lg); line-height: var(--lh-loose); color: var(--ink-soft); font-weight: 300; max-width: 64ch; margin-bottom: var(--s-6); }
  .thanks-forms { display: grid; grid-template-columns: 1fr 1fr; gap: var(--s-4); margin-bottom: var(--s-7); }
  .thanks-form { padding: var(--s-6); background: var(--bone-warm); border: 1px solid var(--bone-edge); border-radius: var(--r-lg); }
  .thanks-form-t { font-family: var(--font-display); font-weight: 400; font-size: var(--t-xl); color: var(--ink); margin-bottom: var(--s-3); }
  .thanks-form p { font-size: var(--t-sm); color: var(--ink-soft); line-height: var(--lh-loose); }
  .paths-choose { font-size: var(--t-base); color: var(--ink); margin-bottom: var(--s-4); }
  .path-list { display: flex; flex-direction: column; gap: var(--s-4); list-style: none; padding: 0; margin: 0; }
  .path-list li { position: relative; padding-left: var(--s-5); font-size: var(--t-base); color: var(--ink-soft); line-height: var(--lh-loose); }
  .path-list li::before { content: ''; position: absolute; left: 0; top: 11px; width: 6px; height: 6px; border-radius: 50%; background: var(--peach); }
  .path-list strong { color: var(--ink); font-weight: 500; }
  .path-list em { color: var(--ink-muted); }
  .paths-switch { margin-top: var(--s-5); font-size: var(--t-sm); color: var(--ink-muted); }
  @media (max-width: 640px) { .thanks-forms { grid-template-columns: 1fr; } }
  .tier-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--s-4); }
  .ftier { padding: var(--s-6); background: var(--bone-warm); border: 1px solid var(--bone-edge); border-radius: var(--r-lg); display: flex; flex-direction: column; gap: var(--s-2); cursor: pointer; transition: transform var(--d-base) var(--ease-out), border-color var(--d-fast) var(--ease-out); }
  .ftier:hover { transform: translateY(-3px); border-color: var(--peach); }
  .ftier:focus-visible { outline: 2px solid var(--peach); outline-offset: 2px; }
  .ftier-choose { margin-top: var(--s-3); font-size: var(--t-xs); letter-spacing: 0.08em; text-transform: uppercase; font-weight: 500; color: var(--peach-deep); }
  .ftier:last-child { grid-column: 1 / -1; }
  .ftier-from { font-family: var(--font-mono); font-size: var(--t-xs); letter-spacing: 0.12em; text-transform: uppercase; color: var(--peach-deep); }
  .ftier-name { font-family: var(--font-display); font-weight: 400; font-size: var(--t-2xl); color: var(--ink); }
  .ftier-essence { font-family: var(--font-display); font-style: italic; font-size: var(--t-lg); color: var(--ink-muted); margin-bottom: var(--s-2); }
  .ftier-benefits { display: flex; flex-direction: column; gap: var(--s-2); list-style: none; padding: 0; margin: 0; }
  .ftier-benefits li { position: relative; padding-left: var(--s-5); font-size: var(--t-sm); color: var(--ink-soft); line-height: var(--lh-normal); }
  .ftier-benefits li::before { content: ''; position: absolute; left: 0; top: 9px; width: 6px; height: 6px; border-radius: 50%; background: var(--peach); }
  .ftier-benefits--exp li::before { background: transparent; border: 1.5px solid var(--peach); width: 5px; height: 5px; }
  .ftier-sub { display: flex; flex-wrap: wrap; align-items: baseline; gap: var(--s-2); margin-top: var(--s-3); }
  .ftier-sublabel { font-family: var(--font-mono); font-size: var(--t-2xs); letter-spacing: 0.1em; text-transform: uppercase; color: var(--ink); }
  .ftier-subnote { font-size: var(--t-2xs); color: var(--ink-faint); }
  .tier-foot { font-size: var(--t-sm); color: var(--ink-muted); margin-top: var(--s-6); max-width: 70ch; line-height: var(--lh-loose); }

  .give-paths { display: grid; grid-template-columns: 1fr 1fr; gap: var(--s-4); }
  .give-path { padding: var(--s-6); background: var(--bone-warm); border: 1px solid var(--bone-edge); border-radius: var(--r-lg); cursor: pointer; display: flex; flex-direction: column; transition: transform var(--d-base) var(--ease-out), border-color var(--d-fast) var(--ease-out); }
  .give-path:hover { transform: translateY(-3px); border-color: var(--peach); }
  .give-path:focus-visible { outline: 2px solid var(--peach); outline-offset: 2px; }
  .give-path-cta { margin-top: var(--s-4); font-size: var(--t-xs); letter-spacing: 0.08em; text-transform: uppercase; font-weight: 500; color: var(--peach-deep); }
  .give-path-t { font-family: var(--font-display); font-weight: 400; font-size: var(--t-xl); color: var(--ink); margin-bottom: var(--s-3); }
  .give-path p { font-size: var(--t-sm); color: var(--ink-soft); line-height: var(--lh-loose); }
  @media (max-width: 640px) { .give-paths { grid-template-columns: 1fr; } }

  .fband .band-center { text-align: center; }
  .fband .band-center p { font-size: var(--t-lg); color: color-mix(in srgb, var(--bone) 90%, transparent); font-weight: 300; line-height: var(--lh-loose); margin-top: var(--s-4); }

  .steps { display: grid; grid-template-columns: repeat(5, 1fr); gap: var(--s-4); }
  .step { display: flex; flex-direction: column; gap: var(--s-2); }
  .step-n { font-family: var(--font-mono); font-size: var(--t-sm); color: var(--peach-deep); }
  .step-t { font-family: var(--font-display); font-weight: 400; font-size: var(--t-md); color: var(--ink); }
  .step p { font-size: var(--t-sm); color: var(--ink-muted); line-height: var(--lh-normal); }

  .cal { width: 100%; border-collapse: collapse; font-size: var(--t-sm); }
  .cal th { text-align: left; font-family: var(--font-mono); font-size: var(--t-2xs); letter-spacing: 0.1em; text-transform: uppercase; color: var(--ink-faint); padding: 0 var(--s-3) var(--s-3); border-bottom: 1px solid var(--bone-edge); }
  .cal td { padding: var(--s-3); border-bottom: 1px solid var(--bone-edge); color: var(--ink-soft); }
  .cal-m { font-weight: 500; color: var(--ink); white-space: nowrap; }
  .cal-a { color: var(--ink-muted); white-space: nowrap; }
  .cal-foot { font-size: var(--t-sm); color: var(--ink-muted); margin-top: var(--s-5); max-width: 70ch; line-height: var(--lh-loose); }

  .faqs { display: flex; flex-direction: column; gap: var(--s-6); }
  .faq-q { font-family: var(--font-display); font-weight: 400; font-size: var(--t-lg); color: var(--ink); margin-bottom: var(--s-2); }
  .faq-a { font-size: var(--t-base); color: var(--ink-muted); line-height: var(--lh-loose); }

  .form-section { padding-top: var(--s-3); }
  .form-lede { font-size: var(--t-lg); color: var(--ink-soft); font-weight: 300; margin-bottom: var(--s-6); max-width: 56ch; }
  .contact-line { margin-top: var(--s-6); color: var(--ink-muted); font-size: var(--t-base); }
  .contact-line a { color: var(--peach-deep); }

  @media (max-width: 760px) {
    .tier-grid { grid-template-columns: 1fr; }
    .ftier:last-child { grid-column: auto; }
    .steps { grid-template-columns: 1fr 1fr; }
  }
</style>
